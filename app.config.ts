export default defineAppConfig({
  ui: {
    colors: {
      primary: "purple",
      neutral: "zinc",
    },
    container: {
      base: "w-full max-w-(--ui-container) h-100vh overflow-y-auto mx-auto p-2 sm:p-4 lg:p-4",
    },
  },
});
